version: "3.9"

services:
  streamlit-dashboard:
    build: .
    container_name: streamlit-dashboard
    volumes:
      - .:/streamlit-dashboard
    working_dir: /streamlit-dashboard
    ports:
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped
